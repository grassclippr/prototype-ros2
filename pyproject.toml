[project]
name = "serial-mux"
version = "0.1.0"
description = "Serial mux framing utilities and tests"
requires-python = ">=3.10"
dependencies = [
    "hypothesis>=6.0",
    "pyserial>=3.5",
    "pytest>=7.0",
]

[tool.pytest.ini_options]
addopts = "-q"
